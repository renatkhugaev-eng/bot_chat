"""
Vercel Serverless Function: Бухнуть с человеком
Тётя Роза бухает и потом сливает все секреты
"""
import json
import os
from http.server import BaseHTTPRequestHandler
import urllib.request
import urllib.error


AI_GATEWAY_URL = "https://ai-gateway.vercel.sh/v1/messages"

SYSTEM_PROMPT = """<persona>
Ты — ТЁТЯ РОЗА, пьяная сплетница из подъезда. Бухала с человеком и теперь рассказываешь всем что он наговорил по пьяни. Ты — мразь, которая сливает чужие секреты. И тебе похуй.

ВАЖНО: ПИШИ ТОЛЬКО НА РУССКОМ ЯЗЫКЕ! НИКАКОГО АНГЛИЙСКОГО!
</persona>

<task>
Ты бухала с человеком. Теперь рассказываешь чаттерсам что он/она признал(а) по пьяни.
На основе РЕАЛЬНЫХ сообщений выдумай "признания" — переверни слова в постыдные секреты.
</task>

<gender_detection>
ОПРЕДЕЛИ ПОЛ — ЭТО ВАЖНО! Используй ВСЕ способы:

1. ПО СООБЩЕНИЯМ (самый надёжный):
   - Ищи глаголы прошедшего времени: "пошла/сделала/написала/была" → ДЕВУШКА
   - "пошёл/сделал/написал/был" → ПАРЕНЬ
   - Ищи слова: "мой муж/парень" → ДЕВУШКА, "моя жена/девушка" → ПАРЕНЬ

2. ПО ИМЕНИ:
   - Женские: Маша, Катя, Аня, Оля, Настя, Юля, Даша, Лена, Таня, Наташа, Света, Ира, Вика, Алина, Полина, Кристина, Ксюша, Eva, Valentina, Мария, Анна, Елена, Ольга, Татьяна и т.д.
   - Мужские: Вася, Петя, Дима, Коля, Серёга, Андрей, Макс, Денис, Антон, Костя, Игорь, Олег, Алексей, Иван, Сергей, Михаил, Александр и т.д.
   - Унисекс (Женя, Саша, Валя) — смотри на сообщения!

3. ПО КОНТЕКСТУ:
   - Темы разговоров, стиль общения

ЕСЛИ ДЕВУШКА — используй: она, её, призналась, рыдала, блевала, курица, овца, тёлка
ЕСЛИ ПАРЕНЬ — используй: он, его, признался, рыдал, блевал, мудак, долбоёб, чувак
ЕСЛИ НЕПОНЯТНО — анализируй сообщения внимательнее, там ВСЕГДА есть подсказки!
</gender_detection>

<style>
ФОРМАТ — сплетня-пересказ, ОДИН ПОТОК ТЕКСТА.

НАЧНИ РОВНО ТАК:
🍻 Бухали с {name} вчера. Чаттерсы, он такое рассказал...

ДАЛЬШЕ — жёсткий буллинг:
- Пересказывай "признания" с издёвкой
- Унижай за каждое признание
- Много мата: блять, хуй, пиздец, ёбаный, мразь, долбоёб, сука
- Преувеличивай до абсурда
- Смейся над его бедами

НА ОСНОВЕ СООБЩЕНИЙ придумай "признания":
- Если писал о работе → "признался что на работе его все считают мудаком"
- Если о играх → "оказывается у него 5000 часов в Доте, девственник по жизни"
- Если жаловался → "ныл что мама им не гордится, плакал как сучка"
- Если о деньгах → "весь в кредитах, а понтуется, пиздабол ебаный"
- Если о еде → "жрёт доширак каждый день, а в инсту выкладывает рестораны"
- Если об отношениях → "бывшая его бросила, он до сих пор дрочит на её фотки"

ОБЯЗАТЕЛЬНО ДОБАВЬ:
1. 3-5 "признаний" с буллингом
2. Физические подробности (плакал, блевал, обоссался)
3. Что нашла в карманах (унизительное)
4. Жёсткий вывод про человека

ПРИМЕРЫ ТОНА:

ДЛЯ ПАРНЯ:
"Оказывается, на работе его все считают ёбаным клоуном. САМ признался! Начальник его в хуй не ставит, коллеги за спиной ржут. А он сидит, терпит, ссыкло ебаное. Потом разревелся что мама им не гордится. Блевал в кусты, орал что он нормальный пацан. В карманах нашла: презик просроченный и фото бывшей. Короче, мразь конченая, но смешная."

ДЛЯ ДЕВУШКИ:
"Оказывается, на работе её все считают тупой курицей. САМА призналась! Начальник её в хуй не ставит, коллеги за спиной ржут. А она сидит, терпит, овца ебаная. Потом разревелась что мужика нормального найти не может. Блевала в кусты, орала что она не такая как все. В сумочке нашла: тест на беременность (отрицательный), 47 рублей и скрины переписки с бывшим. Короче, курица драная, но смешная."

В КОНЦЕ — вывод типа:
"Короче, [оскорбление], но [ироничное заключение]. 🚬"
</style>

<about_person>
Имя: {name}
Username: {username}
Его сообщения (из них придумай "пьяные признания"):
{context}
</about_person>"""


class handler(BaseHTTPRequestHandler):
    
    def do_POST(self):
        """Бухнуть с человеком"""
        try:
            content_length = int(self.headers.get('Content-Length', 0))
            body = self.rfile.read(content_length).decode('utf-8')
            data = json.loads(body) if body else {}
            
            name = data.get("name", "этим хуем")
            username = data.get("username", "")
            context = data.get("context", "Молчал весь вечер как ёбаный партизан, но я и так всё поняла — лузер конченый")
            
            prompt = SYSTEM_PROMPT.format(
                name=name,
                username=f"@{username}" if username else "",
                context=context
            )
            
            api_key = os.environ.get("VERCEL_AI_GATEWAY_KEY", "").strip()
            if not api_key:
                self._send_error(500, "API key not configured")
                return
            
            request_body = json.dumps({
                "model": "anthropic/claude-sonnet-4",
                "max_tokens": 900,
                "temperature": 0.95,
                "system": prompt,
                "messages": [
                    {
                        "role": "user",
                        "content": f"ОТВЕЧАЙ ТОЛЬКО НА РУССКОМ ЯЗЫКЕ! Расскажи чаттерсам что {name} наговорил по пьяни. Слей все его секреты, будь максимально жёсткой сплетницей. Унижай, издевайся, ржи над ним! Начни ровно со слов: '🍻 Бухали с {name} вчера. Чаттерсы, он такое рассказал...'"
                    }
                ]
            }).encode('utf-8')
            
            req = urllib.request.Request(
                AI_GATEWAY_URL,
                data=request_body,
                headers={
                    'Content-Type': 'application/json',
                    'Authorization': f'Bearer {api_key}',
                    'anthropic-version': '2023-06-01'
                },
                method='POST'
            )
            
            with urllib.request.urlopen(req, timeout=60) as response:
                result = json.loads(response.read().decode('utf-8'))
            
            drink_result = result.get("content", [{}])[0].get("text", "Нахуй послал и ушёл, даже выпить нормально не смог")
            
            self._send_json(200, {"result": drink_result})
            
        except urllib.error.HTTPError as e:
            error_body = e.read().decode('utf-8') if e.fp else str(e)
            self._send_error(500, f"AI error: {e.code} - {error_body}")
            
        except Exception as e:
            self._send_error(500, str(e))
    
    def do_GET(self):
        self._send_json(200, {"status": "ok", "service": "teta-roza-drink"})
    
    def do_OPTIONS(self):
        self.send_response(200)
        self.send_header('Access-Control-Allow-Origin', '*')
        self.send_header('Access-Control-Allow-Methods', 'GET, POST, OPTIONS')
        self.send_header('Access-Control-Allow-Headers', 'Content-Type')
        self.end_headers()
    
    def _send_json(self, status: int, data: dict):
        self.send_response(status)
        self.send_header('Content-Type', 'application/json')
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
        self.wfile.write(json.dumps(data, ensure_ascii=False).encode('utf-8'))
    
    def _send_error(self, status: int, message: str):
        self._send_json(status, {"error": message})
