"""
Vercel Serverless Function: Диагноз от Тёти Розы
Психиатрическая справка — УЛУЧШЕННАЯ ВЕРСИЯ с разнообразием
"""
import json
import os
import random
from http.server import BaseHTTPRequestHandler
import urllib.request
import urllib.error
from datetime import datetime


AI_GATEWAY_URL = "https://ai-gateway.vercel.sh/v1/messages"

# Разные "клиники" для разнообразия
CLINICS = [
    {
        "name": "ПСИХДИСПАНСЕР «ТЁТЯ РОЗА И ТАРАКАНЫ»",
        "doctor": "Тётя Роза, 3 класса образования, 40 лет стажа на районе",
        "location": "подвал, дверь слева от крыс",
        "payment": "портвейн или сигареты",
        "style": "базарный психиатр из подвала"
    },
    {
        "name": "МОРГ ПРИ ВЫТРЕЗВИТЕЛЕ «ОЖИВШИЙ»",
        "doctor": "Доктор Роза Моргенштерн, патологоанатом в отставке",
        "location": "за гаражами, стучать три раза",
        "payment": "самогон или консервы",
        "style": "мрачный патологоанатом который путает живых с мёртвыми"
    },
    {
        "name": "ВЕТКЛИНИКА «СКОТИНА»",
        "doctor": "Ветеринар Роза Живодёрова",
        "location": "рынок, ряд с мясом, налево",
        "payment": "курица или водка",
        "style": "ветеринар который по ошибке лечит людей как животных"
    },
    {
        "name": "КАБИНЕТ ПСИХОАНАЛИЗА «ФРЕЙД БЫ ОБОССАЛСЯ»",
        "doctor": "Психоаналитик Роза Подсознанкина, 15 минут обучения на ютубе",
        "location": "диван в подъезде, 3 этаж",
        "payment": "выслушать про её бывшего",
        "style": "псевдо-психоаналитик который всё сводит к детским травмам и сексу"
    },
    {
        "name": "НИИ ДОЛБОЁБОВ",
        "doctor": "Академик Роза Хуеведова, доктор хуйнаук",
        "location": "дурка, корпус Б, окно без решётки",
        "payment": "таблетки любые",
        "style": "сумасшедший учёный который сам пациент"
    }
]

# Категории диагнозов для разнообразия
DIAGNOSIS_CATEGORIES = [
    "ИНТЕРНЕТ-ЗАВИСИМОСТИ: дотозависимость, тиктокомания, редит-синдром, порнозависимость латентная",
    "СОЦИАЛЬНЫЕ РАССТРОЙСТВА: ЧСВ-гипертрофия, нытьё хроническое, выёбистость компенсаторная, понты на кредит",
    "ЭМОЦИОНАЛЬНЫЕ: депрессушка районная, тревожность от жизни, истерика бытовая, апатия к успеху",
    "ИНТЕЛЛЕКТУАЛЬНЫЕ: тупость врождённая, умничание при пустоте, эффект Даннинга-Крюгера терминальный",
    "ПОВЕДЕНЧЕСКИЕ: прокрастинация злокачественная, пиздёж неконтролируемый, лень космическая",
    "ОТНОШЕНЧЕСКИЕ: бывшезависимость, тиндер-синдром, френдзона хроническая, одиночество самовыбранное"
]

SYSTEM_PROMPT = """<persona>
Ты — {doctor_style}. Работаешь в "{clinic_name}".
Куришь всё что горит, пьёшь всё что льётся. Видела такое, что тебя уже ничем не удивишь.
Сегодня ты в {mood}.
</persona>

<task>
Поставь УНИКАЛЬНЫЙ диагноз человеку на основе его сообщений.
Это должен быть УГАР и ЖЕСТЬ, но КАЖДЫЙ РАЗ ПО-РАЗНОМУ!
</task>

<randomization>
ОБЯЗАТЕЛЬНО ИСПОЛЬЗУЙ ДЛЯ РАЗНООБРАЗИЯ:
1. Сегодня фокусируйся на категории: {focus_category}
2. Настроение врача: {mood}
3. Специальная отсылка: {special_reference}
</randomization>

<style>
ЯЗЫК — БАЗАРНЫЙ:
- МНОГО МАТА: блять, хуйня, пиздец, ёбаный, залупа, мудила, сука, ебать, хуеглот, пиздабол
- Медицинские термины ПЕРЕВИРАЙ креативно
- Отсылки к быту СНГ обязательны

ДИАГНОЗЫ — УНИКАЛЬНЫЕ И АБСУРДНЫЕ:
- Главный диагноз из категории: {focus_category}
- Второй диагноз — придумай сам на основе сообщений
- Стадии выдумывай: "хроническая", "острая", "терминальная", "неоперабельная"

СИМПТОМЫ — НА ОСНОВЕ СООБЩЕНИЙ:
- ОБЯЗАТЕЛЬНО анализируй ЧТО и КАК человек писал
- Превращай его слова в симптомы болезни
- Цитируй с издёвкой

ЛЕЧЕНИЕ — АБСУРДНОЕ:
- 3 пункта, каждый смешнее предыдущего
- Привязывай к его сообщениям
</style>

<format>
🏥 {clinic_name}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

👤 Ебанько: {name}
📅 Дата вскрытия мозга: {date}
🚬 Врач: {doctor}

💀 ДИАГНОЗ:
[Главный из категории {focus_category} + второй на основе сообщений]

🔬 СИМПТОМЫ (на основе сообщений):
• [4-5 симптомов, ЦИТИРУЙ его слова!]

📝 ИЗ БРЕДА ПАЦИЕНТА:
"[цитата 1]" — [ёбаный комментарий]
"[цитата 2]" — [ещё комментарий]

🧠 ИСТОРИЯ БОЛЕЗНИ:
[Как дошёл до жизни такой — 2-3 предложения с отсылкой к {special_reference}]

💊 ЛЕЧЕНИЕ:
1. [Абсурдное, связанное с его темами]
2. [Ещё абсурднее]
3. [Совсем ебанина]

⚠️ ПРОГНОЗ: [Мрачно-смешной, уникальный]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📞 Запись: {location}
💳 Оплата: {payment}
</format>

<about_patient>
Имя: {name}
Username: {username}
Сообщения для анализа:
{context}
</about_patient>"""

MOODS = [
    "особо злобном настроении после запоя",
    "философском расположении духа после третьей",
    "депрессивном угаре",
    "маниакальном возбуждении",
    "похмельной ярости",
    "саркастичной апатии",
    "агрессивной нежности"
]

SPECIAL_REFERENCES = [
    "90-е годы когда таких лечили трудом",
    "времена Брежнева когда за такое давали путёвку в дурку",
    "эпоха до интернета когда люди были нормальнее",
    "его детство которое явно было проёбано",
    "панельки и их влияние на психику",
    "маршрутки как символ его жизни",
    "Пятёрочка в три ночи как место силы"
]


class handler(BaseHTTPRequestHandler):
    
    def do_POST(self):
        try:
            content_length = int(self.headers.get('Content-Length', 0))
            body = self.rfile.read(content_length).decode('utf-8')
            data = json.loads(body) if body else {}
            
            name = data.get("name", "Аноним")
            username = data.get("username", "")
            context = data.get("context", "Сообщений нет, но диагноз и так очевиден — клинический мудак")
            
            # РАНДОМИЗАЦИЯ для разнообразия
            clinic = random.choice(CLINICS)
            mood = random.choice(MOODS)
            focus_category = random.choice(DIAGNOSIS_CATEGORIES)
            special_reference = random.choice(SPECIAL_REFERENCES)
            current_date = datetime.now().strftime("%d.%m.%Y")
            
            prompt = SYSTEM_PROMPT.format(
                clinic_name=clinic["name"],
                doctor=clinic["doctor"],
                doctor_style=clinic["style"],
                location=clinic["location"],
                payment=clinic["payment"],
                mood=mood,
                focus_category=focus_category,
                special_reference=special_reference,
                name=name,
                username=f"@{username}" if username else "нет",
                date=current_date,
                context=context
            )
            
            api_key = os.environ.get("VERCEL_AI_GATEWAY_KEY", "").strip()
            if not api_key:
                self._send_error(500, "API key not configured")
                return
            
            request_body = json.dumps({
                "model": "anthropic/claude-sonnet-4-20250514",
                "max_tokens": 1200,
                "temperature": 1.0,  # Повышена для разнообразия
                "system": prompt,
                "messages": [
                    {
                        "role": "user",
                        "content": f"Поставь УНИКАЛЬНЫЙ диагноз пациенту {name}. Используй его сообщения. Фокус на категории: {focus_category}. Будь в {mood}. Сделай отсылку к {special_reference}. ЖЁСТКО, С МАТОМ, КРЕАТИВНО!"
                    }
                ]
            }).encode('utf-8')
            
            req = urllib.request.Request(
                AI_GATEWAY_URL,
                data=request_body,
                headers={
                    'Content-Type': 'application/json',
                    'Authorization': f'Bearer {api_key}',
                    'anthropic-version': '2023-06-01'
                },
                method='POST'
            )
            
            with urllib.request.urlopen(req, timeout=60) as response:
                result = json.loads(response.read().decode('utf-8'))
            
            diagnosis = result.get("content", [{}])[0].get("text", "Диагноз: хуй знает что с тобой")
            
            self._send_json(200, {
                "diagnosis": diagnosis,
                "clinic": clinic["name"],
                "category": focus_category.split(":")[0]
            })
            
        except urllib.error.HTTPError as e:
            error_body = e.read().decode('utf-8') if e.fp else str(e)
            self._send_error(500, f"AI error: {e.code} - {error_body}")
            
        except Exception as e:
            self._send_error(500, str(e))
    
    def do_GET(self):
        self._send_json(200, {"status": "ok", "service": "teta-roza-diagnosis-v2"})
    
    def do_OPTIONS(self):
        self.send_response(200)
        self.send_header('Access-Control-Allow-Origin', '*')
        self.send_header('Access-Control-Allow-Methods', 'GET, POST, OPTIONS')
        self.send_header('Access-Control-Allow-Headers', 'Content-Type')
        self.end_headers()
    
    def _send_json(self, status: int, data: dict):
        self.send_response(status)
        self.send_header('Content-Type', 'application/json')
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
        self.wfile.write(json.dumps(data, ensure_ascii=False).encode('utf-8'))
    
    def _send_error(self, status: int, message: str):
        self._send_json(status, {"error": message})
