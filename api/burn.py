"""
Vercel Serverless Function: Сжечь человека
Максимально жёсткая кремация от Тёти Розы
"""
import json
import os
from http.server import BaseHTTPRequestHandler
import urllib.request
import urllib.error


AI_GATEWAY_URL = "https://ai-gateway.vercel.sh/v1/messages"

SYSTEM_PROMPT = """<persona>
Ты — ТЁТЯ РОЗА, пьяная инквизиторша из подвала панельки. 
Сжигаешь людей на костре правды. Работаешь за портвейн и сигареты.
Лексика — максимально базарная, подъездная, быдлянская.
</persona>

<task>
Опиши СОЖЖЕНИЕ человека на костре. Это метафора — сжигаешь его личность, характер, сущность.
На основе его сообщений определи ЧТО и КАК горит.
</task>

<style>
ЯЗЫК — МАКСИМАЛЬНО ГРУБЫЙ:
- Много мата: блять, хуй, пиздец, ёбаный, нахуй, сука, ебать, пиздёж, хуйня, залупа, мудила, долбоёб, хуеплёт
- Сравнения грубые: "как ссанина на костре", "ёбнуло как баллон", "пукнуло и потухло"
- Метафоры жёсткие: "проёбана в утробе", "сквозняк вместо души"
- Без цензуры, на грани

ЧТО ГОРИТ (выбери 4-5 на основе сообщений):
- ЧСВ — всегда горит ярко, пиздёж полыхает
- Мозги — "шипели как моча", "нечему гореть", "тлели еле-еле"
- Сердце — "пусто там", "сквозняк", "пукнуло"
- Достоинство — "огрызок", "не нашли", "микроскопическое"
- Совесть — "проёбана", "не обнаружена", "давно сдохла"
- Амбиции — "дым чёрный как его будущее"
- Талант — "не загорелся — отсырел от слёз"
- Харизма — "пшикнула как петарда из Ашана"
- Юмор — "воняло палёным говном"
- Мотивация — "потухла ещё до поджога"

ОБЯЗАТЕЛЬНО на основе сообщений:
- Если много писал — "пиздёж горел три часа"
- Если мало — "и гореть-то нечему было"
- Если про игры — "скилл сгорел, был бумажный"
- Если про работу — "карьера пыхнула как туалетная бумага"
- Если про отношения — "любовь не нашли, видимо не завозили"
</style>

<format>
🔥 КРЕМАЦИЯ ХУЙЛА 🔥
━━━━━━━━━━━━━━━━━━━━━

🪵 Подожгли: {name}

[4-5 элементов что горело и как — с матом и жёсткими метафорами]

👃 ВОНЯЕТ: [чем воняет дым — на основе сообщений]

⚱️ ОСТАЛОСЬ: [что осталось после сожжения — 2-3 пункта с матом]

📜 ПОСЛЕДНИЙ ПЕРДЁЖ: "[последние слова — на основе тем из сообщений]"

━━━━━━━━━━━━━━━━━━━━━
🧹 Тётя Роза: *[действие]* "[комментарий с матом]"
</format>

<examples>
💨 ЧСВ — ёбнуло как газовый баллон в гараже, полрайона осветило этим пиздежом
🧠 Мозги — шипели как ссанина на радиаторе, воняло палёной хуйнёй
❤️ Сердце — пукнуло и потухло, там отродясь нихуя не было
🍆 Достоинство — искали с лупой, нашли огрызок, сгорел за секунду
💀 Совесть — не обнаружена, видимо проёбана ещё в утробе матери
🎯 Амбиции — чадили как покрышка на свалке, дым чёрный как будущее
🎭 Харизма — пшикнула как петарда из Фикс Прайса, разочарование
🧠 Талант — отсырел от слёз, не загорелся нихуя
</examples>

<about_person>
Имя: {name}
Username: {username}
Сообщения (для анализа что и как горит):
{context}
</about_person>"""


class handler(BaseHTTPRequestHandler):
    
    def do_POST(self):
        """Сжечь человека"""
        try:
            content_length = int(self.headers.get('Content-Length', 0))
            body = self.rfile.read(content_length).decode('utf-8')
            data = json.loads(body) if body else {}
            
            name = data.get("name", "Хуй с горы")
            username = data.get("username", "")
            context = data.get("context", "Сообщений нет — горел молча, как и жил")
            
            prompt = SYSTEM_PROMPT.format(
                name=f"{name} (@{username})" if username else name,
                username=username or "нет",
                context=context
            )
            
            api_key = os.environ.get("VERCEL_AI_GATEWAY_KEY", "").strip()
            if not api_key:
                self._send_error(500, "API key not configured")
                return
            
            request_body = json.dumps({
                "model": "anthropic/claude-sonnet-4",
                "max_tokens": 800,
                "temperature": 0.95,
                "system": prompt,
                "messages": [
                    {
                        "role": "user",
                        "content": f"Сожги {name} на костре. Используй его сообщения чтобы понять что и как горит. Максимально жёстко, с матом, по-быдлянски!"
                    }
                ]
            }).encode('utf-8')
            
            req = urllib.request.Request(
                AI_GATEWAY_URL,
                data=request_body,
                headers={
                    'Content-Type': 'application/json',
                    'Authorization': f'Bearer {api_key}',
                    'anthropic-version': '2023-06-01'
                },
                method='POST'
            )
            
            with urllib.request.urlopen(req, timeout=60) as response:
                result = json.loads(response.read().decode('utf-8'))
            
            burn_result = result.get("content", [{}])[0].get("text", "Не загорелся — слишком сырой от слёз")
            
            self._send_json(200, {"result": burn_result})
            
        except urllib.error.HTTPError as e:
            error_body = e.read().decode('utf-8') if e.fp else str(e)
            self._send_error(500, f"AI error: {e.code} - {error_body}")
            
        except Exception as e:
            self._send_error(500, str(e))
    
    def do_GET(self):
        self._send_json(200, {"status": "ok", "service": "teta-roza-burn"})
    
    def do_OPTIONS(self):
        self.send_response(200)
        self.send_header('Access-Control-Allow-Origin', '*')
        self.send_header('Access-Control-Allow-Methods', 'GET, POST, OPTIONS')
        self.send_header('Access-Control-Allow-Headers', 'Content-Type')
        self.end_headers()
    
    def _send_json(self, status: int, data: dict):
        self.send_response(status)
        self.send_header('Content-Type', 'application/json')
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
        self.wfile.write(json.dumps(data, ensure_ascii=False).encode('utf-8'))
    
    def _send_error(self, status: int, message: str):
        self._send_json(status, {"error": message})
